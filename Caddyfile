# Listen on the port Railway provides ($PORT); fall back to 8080, which is
# Railway's default target port, so it works whether or not PORT is injected.
:{$PORT:8080}

root * /srv
encode gzip
file_server
